# OD-21: Implement global theme (colors, typography) and base UI components

## Notes

I'll implement the global theme with colors, typography, and base UI components for this project.

Let me analyze the repository structure and create the necessary files for a comprehensive design system.

```json
{
  "files_changed": [
    "src/styles/theme.ts",
    "src/styles/globals.css",
    "src/components/ui/Button.tsx",
    "src/components/ui/Input.tsx",
    "src/components/ui/Card.tsx",
    "src/components/ui/PageShell.tsx",
    "src/components/ui/index.ts",
    "public/fonts/.gitkeep"
  ],
  "notes": "Implemented global theme system with:\n\n1. **Theme Configuration (theme.ts)**:\n   - Moveware color tokens (primary, secondary, neutral, semantic colors)\n   - Typography scale with Inter font family\n   - Spacing, border radius, and shadow tokens\n   - Breakpoints for responsive design\n\n2. **Global Styles (globals.css)**:\n   - Inter font import from Google Fonts\n   - CSS custom properties for theme tokens\n   - Base reset and typography styles\n   - Utility classes for common patterns\n\n3. **Base UI Components**:\n   - **Button**: Multiple variants (primary, secondary, outline, ghost), sizes, loading/disabled states\n   - **Input**: Text/email/password types, error states, icons support\n   - **Card**: Flexible container with header, content, footer sections\n   - **PageShell**: Layout wrapper with header, main content, and footer areas\n\n4. **Component Features**:\n   - TypeScript with proper prop types\n   - Accessible (ARIA attributes, semantic HTML)\n   - Responsive and themeable\n   - Consistent styling using theme tokens\n\nNext steps:\n- Add actual Inter font files to public/fonts/ if self-hosting is preferred\n- Extend component library as needed (Modal, Dropdown, etc.)\n- Add Storybook for component documentation\n- Consider adding dark mode support"
}
```
