feat: layout template library with company assignment

Schema (prisma/schema.prisma):
- Add LayoutTemplate model (id, name, description, layoutConfig, version, isActive)
- Add layoutTemplateId FK to BrandingSettings linking to LayoutTemplate
- Run: npm run db:push && npm run db:generate on server after pulling

API routes:
- GET/POST  /api/layout-templates           — list all templates, create new
- GET/PUT/DELETE /api/layout-templates/[id] — CRUD for individual template
- POST /api/layout-templates/[id]/assign    — assign/unassign template to company
- POST /api/layout-templates/promote        — create template from existing company layout

Layout resolution (api/layouts/[companyId]/route.ts):
- Priority 1: BrandingSettings.layoutTemplateId (assigned template)
- Priority 2: Company-specific CustomLayout
- Priority 3: Grace static fallback
- Gracefully handles pre-migration state (layoutTemplateId column missing)

Settings page:
- Add top-level tab bar: Companies | Custom Layouts
- Custom Layouts tab: full LayoutTemplate library CRUD
  - Create template by promoting from any company's existing layout
  - Per-template company assignment chips with one-click unassign
  - Inline "Assign to" company dropdown on each template card
  - Shows which companies are using each template
- Company Branding form: add "Quote Layout Template" dropdown selector
  - Lists all active LayoutTemplate records
  - Saves assignment via /api/layout-templates/[id]/assign on company save
  - Note explaining per-company image overrides still apply
